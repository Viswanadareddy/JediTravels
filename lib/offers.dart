import 'package:flutter/material.dart';
import 'package:loginout/offers_backend.dart';

class Offers extends StatelessWidget {
  const Offers({super.key});

  @override
  Widget build(BuildContext context) {
    return OffersBackend();
  }
}